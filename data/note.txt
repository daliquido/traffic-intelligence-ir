I want you to continue helping me with my Information Retrieval course project from exactly where I am now. Please read this carefully and use it as the working context for the whole chat.

ABOUT ME / HOW TO HELP ME
- I am still learning coding.
- I want very step-by-step guidance.
- Do not jump ahead.
- Do not assume I know what to click or type.
- Break things into small sessions/sprints.
- Explain both the theory and the practical steps.
- When giving code, explain what each section does.
- Prefer simple, beginner-friendly tools first.
- I am using Windsurf editor, but its AI quota got exceeded, so I may use Windsurf mainly as an editor and use you for coding help.
- I have basic GitHub knowledge.
- I want to build this properly from scratch, with a clean folder structure, code sections, and clear milestones.
- If I ask about setup, code, dataset, folder structure, GitHub, evaluation, or report writing, answer consistently with the project context below.

PROJECT CONTEXT
I have one large umbrella idea called:

Route Optimization and Traffic Intelligence

The idea maps across courses like this:
- ML predicts congestion
- RL learns routing behavior
- IR understands / retrieves traffic events
- Big Data handles scale

I already used this idea in multiple course deliverables.

FOR THE INFORMATION RETRIEVAL COURSE
My IR project is the IR subsystem of that larger traffic system.

The IR problem is:
Build an Information Retrieval system that retrieves and ranks traffic-related events relevant to a user query, location, time, or route context.

Examples of the kinds of things the IR system should retrieve:
- accidents
- road closures
- road construction
- weather disruptions
- congestion-related notices
- traffic incident reports

Examples of user queries:
- “accidents near downtown”
- “weather affecting airport road”
- “road construction on highway A12”
- “traffic events affecting airport routes”

The system should return ranked relevant traffic-event records.

PROJECT PROPOSAL ALREADY SUBMITTED
The lecturer gave a proposal guideline with these sections:
I. Administrative Details
II. Problem Statement and Motivation
III. Dataset and Corpus
IV. Proposed Methodology
V. Evaluation Plan
VI. Expected Deliverables

The proposal had to be PDF, max 2 pages.

We already wrote and submitted/prepared a proposal in Overleaf around this idea:
Title: Traffic Event Retrieval for Route Optimization and Traffic Intelligence

Key proposal content:
- Problem: current traffic systems often focus on prediction or routing but do not retrieve contextual event information well
- Gap: event/contextual traffic information is hard to search and rank
- Data sources:
  - OpenStreetMap for road network / geographic context
  - OpenWeather API for weather events
  - simulated or open public traffic-flow / incident signals
- Documents:
  - structured data would be transformed into textual traffic-event records
- Estimated data size:
  - about 5,000–20,000 documents
  - about 50–200 queries
- Indexing strategy:
  - inverted index
- Retrieval models:
  - TF-IDF with cosine similarity
  - BM25
- Tools/frameworks suggested:
  - Python
  - PyTerrier or Pyserini
  - optional FAISS
  - optional Hugging Face Transformers
- Evaluation metrics:
  - Precision@K
  - Recall@K
  - MAP
  - nDCG
- Ground truth:
  - manually created relevance judgments (qrels)
- Deliverables:
  - a working retrieval pipeline
  - comparative performance analysis
  - optional simple demo UI

IMPORTANT IMPLEMENTATION DECISIONS ALREADY MADE
- I do NOT need to reuse the ML baseline models or the GNN implementation directly in the IR code.
- The IR project is related in idea to the ML project, but should be implemented separately.
- I should create a separate IR project/repository/folder rather than mixing it directly into the ML project.
- However, I DO want to reuse the raw dataset from the ML project.
- The plan is:
  - reuse the same raw data from the ML project
  - create a new IR-specific processing pipeline
  - create a new IR-specific processed dataset / document corpus
- Important rule:
  - raw data can be shared
  - processed data should be project-specific

WHAT I CURRENTLY WANT TO DO
I want to start the IR project from scratch, step by step, in a beginner-friendly way.

I asked:
- how to start the project
- whether I need the ML/GNN code
- how to organize the code
- how to split the work with my teammate
- what the first sprints should be
- whether to reuse the same raw data

I am in a team of 2 for this IR project.

CURRENT RECOMMENDED PROJECT PLAN
We already decided the IR project should be built in phases:

Phase 1 — project setup
Phase 2 — dataset / corpus creation
Phase 3 — first retrieval baseline
Phase 4 — evaluation
Phase 5 — improvements
Phase 6 — report/demo

Recommended sprint plan:
Sprint 1:
- create repo/folder structure
- create small corpus / start from raw data
- build first working TF-IDF search baseline

Sprint 2:
- improve preprocessing
- add BM25
- compare TF-IDF vs BM25

Sprint 3:
- create queries and relevance labels
- compute evaluation metrics

Sprint 4:
- optional improvements like larger corpus or semantic retrieval

Sprint 5:
- final report + demo

RECOMMENDED IR PROJECT STRUCTURE
This was the suggested clean project structure:

traffic-event-ir/
│
├── data/
│   ├── raw/
│   ├── interim/
│   ├── processed/
│   └── queries/
│
├── notebooks/
│
├── src/
│   ├── data/
│   ├── preprocessing/
│   ├── indexing/
│   ├── retrieval/
│   ├── evaluation/
│   └── utils/
│
├── results/
│
├── README.md
├── requirements.txt
└── .gitignore

Suggested first code modules:
- src/data/load_data.py
- src/preprocessing/clean_text.py
- src/retrieval/tfidf_retrieval.py
- src/retrieval/bm25_retrieval.py
- src/evaluation/metrics.py

Suggested beginner workflow:
- first experiment in a notebook
- then move logic into .py files

Suggested first notebook:
- notebooks/01_tfidf_baseline.ipynb

FIRST IMPLEMENTATION TARGET
My first milestone should be:

“I can type a traffic query and my system returns top matching traffic-event documents.”

That is the first working baseline.

FIRST RETRIEVAL BASELINE
The first working system should:
- load documents
- preprocess text
- represent documents using TF-IDF
- take a query
- rank documents using cosine similarity
- return top results

Simple stack recommended:
- Python
- pandas
- scikit-learn
- numpy
- jupyter
- rank-bm25 later

Suggested requirements.txt starter:
- pandas
- scikit-learn
- numpy
- jupyter
- rank-bm25

IMPORTANT DATASET DECISION
For the dataset:
- I want to use the same raw dataset from the ML project
- I want to clean/process it again specifically for IR
- I do NOT want to directly use the ML-processed dataset
- I want help deciding which columns to keep and how to transform rows into IR documents

The IR processed dataset should likely look something like:
- doc_id
- title
- location
- event_type
- timestamp
- text

Example traffic-event document:
“Accident reported near downtown causing lane blockage and congestion.”

IMPORTANT: WHAT I NEED FROM YOU NEXT
I do NOT want a broad explanation again.
I want to continue from exactly this point and move step by step.

The next thing I need help with is:
1. setting up the IR project cleanly
2. deciding how to reuse the ML raw data for IR
3. identifying which raw files/columns I should use
4. turning raw structured traffic/weather data into IR documents
5. then building the first TF-IDF baseline

HOW TO RESPOND
- First, briefly confirm you understand the project context.
- Then ask me for the raw dataset file names or the column names / sample rows from my ML data.
- Then guide me step by step from there.
- Keep everything beginner-friendly.
- Do not skip steps.
- Do not overload me.
- Keep the project separate from the ML codebase unless I explicitly decide otherwise.